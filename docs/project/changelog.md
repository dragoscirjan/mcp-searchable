# Changelog

Project release history is tracked in the repository root `CHANGELOG.md`.

## Recent highlights

- Multi-provider `web_search` support (`duckduckgo`, `google`, `bing`, `brave`).
- Strict runtime validation for tool inputs.
- MkDocs site for installation, usage, reference, and contributor docs.

For full details, see `CHANGELOG.md`.
